define(function (require) {
    var React = require('react');
    var Router = require('react-router');
    var Bootstrap = require('react-bootstrap');

    return React.createClass({

        render: function () {
            return <div>
                <h1>DataCube Visualizer</h1>
            </div>
        }
    });
});